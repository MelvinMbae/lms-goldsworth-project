import { createContext } from "react";



export const userContext = createContext()

// export function UserContext(){

//     const user = userContext()

//     if (user == ? user : throw new Error("context must wrap app to be used globally")
// }